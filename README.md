# Weather App API (FastAPI + SQLite)

## Features

- User Registration & Login
- Weather CRUD (Create, Read, Update, Delete)
- Admin support (via `is_admin` field)

## How to Run

```bash
pip install -r requirements.txt
uvicorn backend.main:app --reload
```
